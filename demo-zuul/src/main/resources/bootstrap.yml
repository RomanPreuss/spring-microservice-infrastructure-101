spring:
  application:
     name: zuul

eureka:
  instance:
    nonSecurePort: ${server.port:1000}
  client:
    serviceUrl:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:8761}/eureka/